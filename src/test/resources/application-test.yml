spring:
  datasource:
    url: jdbc:h2:mem:family-finance-${random.uuid};DB_CLOSE_DELAY=-1
    username: sa
    password:
  jackson:
    time-zone: UTC
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: false

app:
  seed:
    enabled: false
